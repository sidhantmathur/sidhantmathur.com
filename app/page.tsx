import { AppShell } from "@/components/shell/app-shell";

// The homepage IS the interface. The old document-style homepage — hero,
// project cards, about block, experience table — is gone; its content now
// lives in the conversation and the context panel beside it.
export default function Home() {
  return <AppShell />;
}
